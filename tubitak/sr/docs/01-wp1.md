# Project 2, WP1 — SR core, grid contract, bicubic baseline

**Run:** 2026-08-30. **Repository:** `mvy0502/GenCP`, branch `tubitak-tr`, working tree
clean at the start; the only addition is `tubitak/sr/`. `gencp_core/`, `qgis_plugin/`,
`tests/` and the upstream pix2pix tree were not modified — confirmed by
`git status --porcelain` against each. QGIS wiring was not started; it is WP2.

**Machine:** MacBook Pro, Apple M4 Max, 14 cores (10P + 4E), 36 GB unified memory,
macOS 25.5.0. **Interpreter:** `/opt/homebrew/Caskroom/miniforge/base/envs/gencp/bin/python`,
Python 3.11.15.

**Library versions that affect numerics** (standing practice 9; also embedded in every
output's `GENCP_SR_PROVENANCE` tag): **Pillow 12.3.0** — this is the one that does the
resampling — numpy 2.4.6, rasterio 1.4.4, GDAL 3.12.3.
**Random seed:** the bicubic path uses none and records `"random_seed": null,
"stochastic": false`. The only seeded code is the synthetic-fixture generator in
`tests/gate_s.py`, seed **20260830**.

Deliverables: `tubitak/sr/sr_core/` (6 modules, 933 lines), `tubitak/sr/tests/`
(3 verifiers, 590 lines), `tubitak/sr/docs/gate-s-registration.md`, and this report.

---

## 1. The Gate S registration, exactly as registered

Written to `tubitak/sr/docs/gate-s-registration.md` at **01:12:07**, before the first
`sr_core` module existed (`grid.py`, 01:12:41), before the gate existed (`gate_s.py`,
01:18:03), and before any result (`gate_s_selftest.json`, 01:33:19; `smoke_run.json`,
01:33:57). It has not been edited since. Reproduced verbatim:

> # Gate S — the super-resolution grid contract
>
> **Registered:** 2026-08-30, Project 2 WP1, **before Gate S was run and before any
> super-resolved raster existed on disk.** Standing practice 4: predictions are registered
> before outcomes. This file is never edited to match a result; a revision is permitted only
> if an *input measurement* improves and no outcome has been seen, and the earlier version is
> never deleted.
>
> ## 1. What Gate S claims, and what it does not
>
> Gate S asserts that a super-resolved raster sits on **the exact integer refinement of its
> source raster's grid**. It is a claim about georeferencing arithmetic only. It says nothing
> about whether the interpolated pixel values are good, and nothing about whether an
> upsampler is better than any other. Those are separate questions and are not measured here.
>
> ### Gate S is not an analogue of Gate G, and inherits nothing from it
>
> This must be stated because the two look superficially similar and are not.
>
> | | Gate G (Project 1) | Gate S (Project 2) |
> |---|---|---|
> | quantity | placement of *generated* content against an independently warped reference | arithmetic relation between a *source* grid and its refinement |
> | method | FFT cross-correlation of image content, parabolic sub-pixel peak refinement | exact float comparison of affine terms and pixel-centre coordinates |
> | tolerance | 0.05 px bound; measured 0.000181 px | **exact equality, no tolerance** |
> | what a failure means | the mosaic places pixels wrongly | the output raster is not on the grid it claims |
>
> Gate G's 0.000181 px is a measurement of a different quantity by a different method against
> a different reference. **Gate S is new. It has no track record and borrows none.** Its
> credibility rests entirely on the known-false cases in §4 and on nothing else.
>
> ## 2. Sign and index conventions — stated once, never flipped
>
> * Rasters are **north-up and axis-aligned**: the affine's `b` and `d` terms are zero. A
>   rotated or sheared source is refused, not handled. This is checked where the assumption
>   is made (`sr_core.grid.require_north_up`), not assumed at the call site.
> * A pixel is addressed as **(row `i`, column `j`)**, `i` increasing **southward**, `j`
>   increasing **eastward**, both zero-based.
> * For an affine `T = Affine(a, 0, c, 0, e, f)`, the **centre** of source pixel `(i, j)` is
>
>   ```
>   P_src(i, j) = ( a·(j + 0.5) + c ,  e·(i + 0.5) + f )
>   ```
>
>   with `e` negative for a north-up raster.
> * At integer scale `s`, source pixel `(i, j)` is covered by the output block of rows
>   `[s·i, s·i + s)` and columns `[s·j, s·j + s)`. Its **centre** is
>
>   ```
>   P_out(i, j) = ( (a/s)·(s·j + s/2) + c ,  (e/s)·(s·i + s/2) + f )
>   ```
>
> * **Offset is defined as `P_out − P_src`**, in CRS units, **positive east** in `x` and
>   **positive north** in `y`. This sign is used in every number Gate S prints and is not
>   reversed anywhere.
>
> ## 3. The five assertions
>
> Let the source have transform `T_src = Affine(a, 0, c, 0, e, f)`, width `W`, height `H`,
> CRS `K`; let the output have `T_out`, `W_out`, `H_out`, `K_out`; let `s` be the integer
> scale factor.
>
> | # | assertion | predicate |
> |---|---|---|
> | S1 | output CRS identical to source CRS | `K_out == K` |
> | S2 | output pixel size exactly source / s | `T_out.a == a / s` **and** `-T_out.e == -e / s` |
> | S3 | output origin exactly the source origin | `T_out.c == c` **and** `T_out.f == f` |
> | S4 | output size exactly s times the source | `W_out == s·W` **and** `H_out == s·H` |
> | S5 | no shift | `P_out(i, j) == P_src(i, j)` exactly, for every sampled `(i, j)` |
>
> ### Why S2 and S3 are asserted as exact equality rather than with a tolerance
>
> `s` is constrained to a **power of two** (`sr_core.grid.require_integer_scale`). Under
> IEEE-754 binary64, dividing by a power of two only decrements the exponent: it is exact,
> with no rounding, for every finite non-subnormal operand. So `a / s` is the exact real
> quotient, and a correct implementation reproduces it bit-for-bit. A tolerance would
> therefore admit only *incorrect* implementations, and a half-pixel error at 10 m — 5 m — is
> enormous compared with any tolerance one would be tempted to write. **No tolerance is
> used.** Origins are copied unchanged, so S3 is exact by construction and any inequality is
> a real defect.
>
> S5 is predicted to hold exactly for the same reason. Writing out the algebra with
> `b = d = 0`:
>
> ```
> P_out.x = (a/s)·(s·j + s/2) + c        P_src.x = a·(j + 0.5) + c
> ```
>
> `a/s` is exact; `s·j + s/2` is an exactly representable integer-or-half-integer for the
> sizes in use; both products round the same real number `a·(2j+1)/2` to nearest, so they are
> bit-identical, and the identical `c` is then added to both. **Prediction: max |offset| is
> exactly 0.0, not merely small.** If it is small-but-nonzero, that is a finding to report,
> not a threshold to widen.
>
> **Sampling for S5.** All four corners, plus every pixel on a stride of
> `max(1, W // 17)` × `max(1, H // 17)`, plus indices `0, 1, W-2, W-1` and
> `0, 1, H-2, H-1`. The number of pixels actually compared is printed with the result. A
> sample of size zero is a failure, not a pass.
>
> ## 4. Known-false cases — registered before they were run
>
> Standing practice 10 and 11. A check is trusted only after it has been watched reporting a
> case it must reject. Each row states the **predicted** verdict.
>
> | # | case | predicted |
> |---|---|---|
> | KT1 | the real bicubic output of the pipeline under test | **PASS**, all five assertions |
> | KF1 | output transform offset by **half an output pixel** (`c += a/(2s)`, `f -= -e/(2s)`) | **FAIL on S3 and S5**; S1, S2, S4 pass |
> | KF2 | output pixel size `a/s · (1 + 1e-9)` | **FAIL on S2**; S5 also fails, because a pixel size that is wrong by a relative 1e-9 moves the far-corner centre by ~1e-4 m at this raster size, and S5 is exact |
> | KF3 | output at the **wrong scale** (`s·W` asserted, file is `(s/2)·W`) | **FAIL on S2 and S4** |
> | KF4 | output in a **different CRS** (EPSG:4326 tag on the same array) | **FAIL on S1** |
> | DG1 | **no arguments** | **exit 2**, refusing to run. Must not print a verdict. |
> | DG2 | **missing file** | **exit 2 or a named error**, no verdict |
> | DG3 | **empty raster** (0 valid bytes / unreadable body) | **error, no verdict** |
> | DG4 | **single-pixel raster** (1×1 source) | **PASS** — this is a legitimate degenerate input, not a malformed one, and the arithmetic is defined for it. A crash here is a defect in the gate. |
> | DG5 | an **unrecognised argument** (`--scalee=2`) | **exit 2**, via `tubitak/tests/_guard.py::strict_argv` |
>
> **The gate is not trusted unless KF1–KF4 all fail and DG1–DG3, DG5 all refuse to emit a
> verdict.** If Gate S passes on any of KF1–KF4, or emits a verdict for any of DG1–DG3 or
> DG5, then Gate S is not a gate and that is the finding this work package reports, in place
> of a green result.
>
> ## 5. Invariance — what must not change for this result to mean what it claims
>
> Standing practice 1. Gate S's verdict is about the pipeline *as configured below*. Change
> any of these and the registered result no longer describes what is running.
>
> 1. **Scale `s` is an integer power of two**, enforced in `sr_core.grid.require_integer_scale`.
>    S2's exactness argument is a statement about binary floating point and does not survive
>    `s = 3`. At a non-power-of-two scale the exactness claim must be re-derived or replaced
>    by a stated tolerance; it must not be silently inherited.
> 2. **The source is north-up and axis-aligned** (`b == d == 0`), enforced in
>    `sr_core.grid.require_north_up`. Every formula in §2 assumes it.
> 3. **The output origin is copied, never recomputed.** If a future version snaps or
>    re-derives the origin, S3 becomes a real test of that derivation rather than a test of a
>    copy, and its meaning changes.
> 4. **Tiles are laid out and placed in source pixel coordinates, by integer indexing —
>    never by resampling.** An output block for source tile at `(row0, col0)` occupies output
>    rows `[s·row0, …)` and columns `[s·col0, …)` exactly. If a future version places blocks
>    with `rasterio.warp.reproject` (as `gencp_core.mosaic.build` does, because its tiles sit
>    on a *different* grid from its output), the placement stops being exact integer
>    arithmetic and S5 stops being a statement about arithmetic.
> 5. **No distance in metres is used anywhere in the SR grid or tiling path.** Tiling is in
>    pixels; the grid is a scaling of the source affine. This is why `require_metric` is
>    deliberately *not* applied here, and it is what keeps Project 1's recurring
>    metres-into-degrees failure out of this code. If a metre-valued parameter is ever
>    introduced, this invariant is broken and `require_metric` becomes mandatory.
> 6. **The upsampler does not change the grid.** `Upsampler.upsample` takes `H×W×C` and
>    returns `sH×sW×C`. Gate S's arithmetic is independent of *which* upsampler ran, which is
>    the point: it must give the same verdict for `BicubicUpsampler` today and for a trained
>    ONNX model in WP4. If a future upsampler returns a differently-shaped array, S4 fails,
>    and that is the correct outcome rather than something to accommodate.
>
> ## 6. What Gate S deliberately does not assert
>
> * Anything about pixel **values**. A raster of zeros on the correct grid passes Gate S.
>   This is intentional: the grid contract must be separable from image quality, exactly as
>   Gate G separates grid alignment from content placement.
> * That the tiled-and-blended result equals a single whole-image upsample. That is a real
>   property and it is checked, but **it is not part of Gate S** and is reported separately,
>   so that a seam defect cannot be mistaken for a georeferencing defect or vice versa.
> * Anything about nodata handling. Bicubic interpolation mixes the nodata fill value into
>   neighbouring valid pixels at a nodata boundary; that is a known, unaddressed limitation
>   of this WP1 implementation and is recorded as an open item, not as a gate assertion.

---

## 2. Verification — every invocation and its outcome

Command: `python tubitak/sr/tests/gate_s.py --self-test`. Full transcript at
`tubitak/data/sr_gates/gate_s/gate_s_selftest.json`. Exit code **0**.
Source for KT1/KF1–KF4: a 512×512 window at source pixel (4096, 4096) of
`tubitak/data/tiles36SVJ/TCI.tif`.

| # | case | predicted | **observed** | as registered |
|---|---|---|---|---|
| KT1 | real bicubic output | PASS 5/5 | **PASS 5/5.** S5: 400 pixel centres compared, worst offset `dx 0.0, dy 0.0` | yes |
| KF1 | transform offset half an output pixel | FAIL S3, S5 | **FAIL S3, S5.** origin offset `x 2.5, y -2.5`; S5 worst `dx 2.5, dy -2.5` at (0,0) | yes |
| KF2 | pixel size × (1 + 1e-9) | FAIL S2 (and S5) | **FAIL S2, S5.** `x = 5.000000005` vs expected `5.0`; S5 worst `dx 5.115e-06 m` at (511,0) | yes |
| KF3 | built at scale 1, asserted at scale 2 | FAIL S2, S4 | **FAIL S2, S4 and also S5** — a stronger rejection than registered; S5 worst `dx 5115.0 m` | yes |
| KF4 | same pixels tagged EPSG:4326 | FAIL S1 | **FAIL S1** only; S2–S5 pass, correctly | yes |
| DG1 | no arguments | exit 2, no verdict | **exit 2**, no verdict. `gate_s.py: nothing to check.` | yes |
| DG2 | missing file | error, no verdict | **exit 2**, no verdict | yes |
| DG3a | empty (0-byte) raster | error, no verdict | **exit 2**, no verdict | yes |
| DG3b | truncated raster (first 4096 bytes) | error, no verdict | **exit 2**, no verdict | yes |
| DG4 | single-pixel (1×1) source | PASS | **PASS 5/5**, 1 pixel centre compared, offsets `0.0` | yes |
| DG5 | `--scalee=2` | exit 2, no verdict | **exit 2** via `_guard.strict_argv`, no verdict | yes |

**11 of 11 cases behaved as registered. Gate S can fail, and fails only where it should.**

Two honest notes on the registration versus the outcome, neither of which was corrected in
the registration file:

* **KF3 was under-predicted.** The registration said "FAIL on S2 and S4"; S5 also fails,
  because a scale-1 output has 10 m pixels and its block centres are 5115 m from the source
  centres at the far corner. The rejection is stronger than predicted, not weaker.
* **KF2's magnitude estimate was wrong by ~20×.** The registration said the far-corner
  centre would move "~1e-4 m at this raster size". That figure was computed for a full
  10980 px granule; the case actually ran on a 512 px window, where the correct value is
  512 × 10 × 1e-9 ≈ 5.1e-6 m, and 5.115e-06 m is what was measured. The *prediction that S5
  fails* held; the magnitude I wrote alongside it referred to a different raster than the one
  the case used.

### Gate S on the full-scene smoke-run output

`python tubitak/sr/tests/gate_s.py --source=tubitak/data/tiles36SVJ/TCI.tif
--output=tubitak/data/sr_out/36SVJ_TCI_bicubic_x2.tif --scale=2` → **exit 0, PASS 5/5**:

```
[PASS] S1  output EPSG:32636  ==  source EPSG:32636
[PASS] S2  x = 5.0 (expected 5.0), y = 5.0 (expected 5.0)   [exact float equality]
[PASS] S3  origin offset  x 0.0, y 0.0    [output minus source, +E/+N]
[PASS] S4  got 21960 x 21960, expected 21960 x 21960  (source 10980 x 10980)
[PASS] S5  441 pixel centres compared; worst offset dx 0.0, dy 0.0
```

### Additional checks, deliberately kept outside Gate S

**Tiling equivalence** (`tests/tiling_equivalence.py`, exit 0) — tiled and feather-blended
output against a single whole-window upsample of the same 512×512 window, in DN of uint8,
3,145,728 values compared:

| tile px | overlap px | tiles | max abs diff | mean abs diff | n differing |
|---|---|---|---|---|---|
| 256 | 32 | 9 | **0** | 0.00000 | 0 |
| 256 | 8 | 9 | **0** | 0.00000 | 0 |
| 256 | 4 | 9 | 1 | 0.00010 | 330 |
| 256 | 2 | 9 | 6 | 0.00192 | 5,355 |
| 256 | **0** | 4 | **47** | 0.01313 | 20,049 |
| 128 | 32 | 25 | **0** | 0.00000 | 0 |

The overlap-0 row is this check's known-false control and it fires at 47 DN, so a positive
result from the check means something. **This measurement overturned a claim I had written
into `tiles.py` before testing it** — that a 2–4 px overlap suffices because the cubic kernel
reaches only 2 source pixels each side. It does not: the feather ramp spans the whole
overlap, so at a small overlap the very pixels whose neighbourhood was truncated at the tile
edge still carry appreciable weight. The docstring was corrected to the measured numbers.

**Architectural guard** (`tests/test_no_qgis_imports.py`, exit 0) — 6 modules checked, 0
forbidden imports; refuses `qgis`, `PyQt5`, `PyQt6`, `processing` **and `gencp_core`**. Its
known-false control (a module importing `qgis.core` and `gencp_core`) is detected as 2
offences, so the clean verdict is meaningful. Degenerate invocation
(`--whatever`) → exit 2.

**Atomic write under interruption.** A known-good 693,770-byte sentinel was placed at the
destination, a full-scene run was started to that same path and killed 7 s in:

| signal | killed mid-run | destination | leftover `.part` |
|---|---|---|---|
| SIGTERM (rc −15) | yes | **UNCHANGED — intact sentinel**, 693,770 bytes | 1 × 15,200 bytes |
| SIGKILL (rc −9) | yes | **UNCHANGED — intact sentinel**, 693,770 bytes | 1 × 15,200 bytes |

Known-false control: writing 4096 zero bytes in place into the destination is detected as
CHANGED, so the comparison can fail. **My first version of this test was wrong and I caught
it**: it used SIGINT on a job backgrounded from a non-interactive shell, where SIGINT is
ignored, so the run completed normally, correctly replaced the file, and the test reported
"CORRUPTED" against a stale sentinel hash. The defect was in the test, not in
`mosaic.atomic_path`; the corrected test is the table above.

**Output determinism.** Three full-scene runs at `GDAL_CACHEMAX` = 64 MB, 1024 MB and the
default were compared band-by-band:

* **pixel content bit-identical** — pixel-SHA256 `f8a7a677dadf4184693a0bb4cbfdf940` for all
  three, max abs difference **0 DN**;
* **file bytes differ** — 1,275,560,750 bytes at 64 MB against 1,254,567,338 at 1024 MB and
  at the default, with different file hashes.

This matters beyond WP1: Project 1's Gate R asserts **byte identity**. An SR analogue of
Gate R must hash **pixels, not file bytes**, or it will report a false failure whenever
`GDAL_CACHEMAX` differs between two machines — which it does by default, since GDAL sizes
its cache from installed RAM.

---

## 3. Smoke run

Full scene, no window. `python -m sr_core.run --input ../data/tiles36SVJ/TCI.tif
--output ../data/sr_out/36SVJ_TCI_bicubic_x2.tif --scale 2 --method bicubic --json`,
run from `tubitak/sr/`, timed with `/usr/bin/time -l`. Record at
`tubitak/data/sr_out/smoke_run.json`, timing at `tubitak/data/sr_out/smoke_run.time`.

| | |
|---|---|
| input | `tubitak/data/tiles36SVJ/TCI.tif` — S2 L2A TCI, 36SVJ, 2026-04-30, public Copernicus |
| output | `tubitak/data/sr_out/36SVJ_TCI_bicubic_x2.tif` (gitignored) |
| source | 10980 × 10980, 3 band, uint8, nodata 0.0, EPSG:32636, 10 m |
| output | 21960 × 21960, 3 band, uint8, nodata 0.0, EPSG:32636, **5 m** |
| source transform | `(10.0, 0.0, 399960.0, 0.0, -10.0, 4400040.0)` |
| output transform | `(5.0, 0.0, 399960.0, 0.0, -5.0, 4400040.0)` |
| tiles | 529 (23 × 23), tile 512 px, overlap 32 px, stride 480 px |
| **wall clock** | **26.90 s real** (25.35 user, 1.48 sys) |
| **peak RSS** | **2,660,712,448 B = 2.661 GB** |
| **output size on disk** | **1,254,567,338 B = 1.255 GB** |
| **Gate S** | **PASS, 5/5** |
| uncovered output pixels | **0** (the tile layout covers the raster with no gap) |
| clipped output values | 30,084,071 of 1,664,090,112 = **1.808 %** |
| peak accumulator band | 1024 output rows |

**This is a CPU bicubic run.** It is not an inference time and must not later be compared
with one. There is no GPU in this path: no ONNX session, no MPS, no CoreML. Pillow's
resampling is the only compute of consequence.

### Memory: what was measured, and a defect found by measuring it

Peak RSS was not what I expected, so it was measured rather than asserted. Two runs with
**equal output pixel counts but transposed aspect**:

| output | peak RSS |
|---|---|
| 21960 wide × 4096 tall | **2.655 GB** |
| 4096 wide × 21960 tall | **0.953 GB** |

RSS scales with output **width**, not with total size — the signature of a full-width band
buffer. Varying the band height at full width (`GDAL_CACHEMAX=64` to remove GDAL's cache
from the measurement) gave **2.955 / 1.716 / 1.214 GB** for bands of **1024 / 512 / 256**
rows: about **2.42 MB per band row**, against the **0.35 MB** the accumulator itself needs
at width 21960. That factor of seven located the cost in `StreamingMosaic._emit`, which held
roughly seven simultaneous full-band float32 temporaries (`np.full`, the `divide` result,
`np.rint`, `np.clip`, the dtype cast). Rewriting it to normalise in 128-row chunks with
in-place `rint`/`clip` took peak RSS from **4.390 GB to 2.639 GB** at the default cache and
from **2.955 GB to 1.003 GB** with the cache pinned, at unchanged wall clock and with
**bit-identical pixels**.

Along the way a second, real defect was found and fixed: the first `StreamingMosaic`
consumed its band with `self.acc = self.acc[:, n:]`, which is a numpy **view** and therefore
keeps its base buffer alive — the memory was never released, only made unreachable by index.
It is now a fixed-size pre-allocated band with an explicit small tail copy. **Fixing it did
not reduce peak RSS** (it rose slightly, 4.098 → 4.390 GB), because `_emit`'s temporaries
dominated; the claim is that the defect was real and is gone, not that it was the cause.

### Clipping and dtype — the documented choice

Cubic convolution has negative kernel lobes and overshoots at edges. **This implementation
clips to the source dtype's range for integer dtypes and does not clip floating-point
dtypes.** The reason, stated in `sr_core/upsample.py`: the alternative is worse in a
specific way, because an unclipped float cast back to uint8 **wraps**, so an overshoot to
257 becomes 1 — a black pixel at the brightest edge in the scene, which reads as data rather
than as an artefact. Clipping loses the overshoot; wrapping invents a value.

What is lost is counted rather than assumed negligible: **1.808 %** of output values on this
scene (30,084,071 of 1,664,090,112) were altered by clipping. `--no-clip` disables it, and
the CLI help says plainly what then happens. The counts are on the `Upsampler` interface and
are written into every output's provenance tag.

The second clip, inside `StreamingMosaic._emit`, is a different thing and is **not** counted:
a weighted average of in-range values cannot mathematically leave the range, so that clip
only absorbs float32 rounding at the last ulp. Counting it would inflate the reported figure
with pixels nothing meaningful happened to.

### Provenance embedded in the output

`GENCP_SR_PROVENANCE` — a distinct tag key from Project 1's `GENCP_PROVENANCE`, so a
consumer cannot mistake an SR product for a synthetic reference. 30 fields, including
`source_sha256` (a 256 MB prefix, flagged `source_sha256_is_prefix: true` rather than
silently truncated), `resampling_convention`, `block_placement`, `random_seed: null`,
`stochastic: false`, the four library versions, and the clipping counts.

---

## 4. What was copied from `gencp_core`, and how each copy diverges

`gencp_core` was **read, never modified** — `git status --porcelain tubitak/gencp_core` is
empty. Nothing in `sr_core` imports it, and `tests/test_no_qgis_imports.py` enforces that,
so the two cannot silently re-couple.

| `sr_core` symbol | derived from | how it diverges |
|---|---|---|
| `grid.SRGridError` | `extent.ExtentError` | separate type, so a caller catching Project 2's grid errors does not swallow Project 1's extent errors when both are importable in one process |
| `grid.output_grid` | `extent.output_grid` | takes a **source raster** and refines it, not a map extent to snap to: no `ceil`, no east/south overhang, output covers exactly the source footprint. Pixel size comes from the source transform and `scale`, not from the module global `NOMINAL = 10.0` |
| `grid.require_north_up` | `vectors.require_metric` (in spirit) | same discipline — check the assumption where it is made — but a **different assumption**: `b == d == 0`, because every pixel-centre formula here drops the rotation terms. Note that `require_metric` itself is deliberately **not** carried over: `sr_core` adds no distance to any coordinate, so it has no metres-as-degrees entry point |
| `grid.require_integer_scale` | nothing — new | Project 1 has no scale factor. Restricts `s` to a power of two so that S2's exact-equality assertion is sound |
| `grid.source_pixel_centre`, `grid.output_block_centre` | nothing — new | the registered convention, in one place, so the gate and the pipeline cannot disagree about it |
| `tiles.tile_grid` | `extent.tile_grid` | **source pixels, not metres**; tile size is a pixel count, not `TILE_M = 2570`. Edge tiles are **clamped back inside** the raster (gencp_core lets its last tile run past the extent, which is safe when a tile is synthesised and unsafe when it is read from a real raster). Returns explicit per-tile sizes, because clamping makes them vary |
| `mosaic.feather_weight` | `mosaic.feather_weight` | same raised-cosine kernel. `h`/`w` explicit because clamped edge tiles are not square, and **the four sides ramp independently** — a side at the raster boundary has no neighbour and is not ramped, where gencp_core ramps all four and then divides by a weight ~1e-4 of its interior value |
| `mosaic.StreamingMosaic` | `mosaic.build` | same accumulate-then-normalise arithmetic. **Streamed** over a fixed band instead of allocating the whole raster (11.6 GB for one scene at s=2), and blocks are placed by **integer slicing, not `reproject`** — gencp_core must warp because its 10.0390625 m tiles do not land on its 10.0 m output grid, whereas here they land exactly by construction |
| `mosaic.write_geotiff` | `mosaic.write_geotiff` | dtype, band count and nodata come from the **array and the source profile** rather than being fixed at 3-band uint8 / nodata 0; the write is **atomic**; the tag key is `GENCP_SR_PROVENANCE` |
| `mosaic.provenance` | `pipeline.provenance` | identifies a source raster and an upsampler rather than an ONNX model and a vector source, and **records the library versions that affect numerics**, which the Project 1 version does not |
| `mosaic.atomic_path` | nothing — new | temp file in the same directory, fsync, `os.replace`, fsync the directory. Project 1 had no atomic write, and a truncated `.tif` from an interrupted run was once treated as a cache hit |
| `upsample.OnnxGenerator` *pattern* (not code) | `infer.OnnxGenerator`, `infer.generate_tiles` | only the **shape** of the design is reused: a loaded model object, a tiling loop with progress and cancellation. **`infer.preprocess` is deliberately not called and not copied** — its bicubic resize to 256 px is correct for pix2pix and destructive here, since in super-resolution the resampling is exactly what the model must learn |

One deliberate **reuse** rather than a copy: `tubitak/tests/_guard.py::strict_argv` is
imported by all three `tubitak/sr/tests/` verifiers. One definition of "refuse what you do
not understand" is better than two that can drift. Recording it here because CLAUDE.md's
deletion rule asks whether anything in the repository reads a file before it is moved:
`tubitak/tests/_guard.py` now has three readers under `tubitak/sr/`.

**Path check** (CLAUDE.md, mechanical): every repository path referenced by
`tubitak/sr/sr_core`, `tubitak/sr/tests` and `tubitak/sr/docs` was resolved —
**33 paths checked, 0 missing.**

---

## 5. Open items

1. **Bicubic is nodata-blind.** It interpolates the raw array, so at a nodata boundary the
   fill value is mixed into neighbouring valid pixels over the kernel support. The five TCI
   scenes use nodata 0, and two of them carry substantial nodata: in WP0's 256 px chip screen
   (`00-recon.md` §3.1, path `tubitak/data/{tile}/SCL.tif`), **440 of 1764 chips in 36SWJ and
   122 of 1764 in 36TUK were rejected for nodata** — that is a fraction of *chips touching*
   nodata, not a nodata pixel fraction, and the pixel fraction has not been measured. On
   36SVJ the question is nearly moot because the granule is full. A mask-aware upsample, or
   at minimum a measurement of the affected border width, is needed before any scene with
   real nodata is super-resolved for evaluation.
2. **A source smaller than one tile in either axis breaks a static-shape model.** Measured:
   a stub upsampler with a trained ONNX graph's constraints runs the full scene, a 1024×1024
   window and a 1000×1000 window (all tiles exactly 512²), and fails on a 300×300 window with
   `input shape (1, 3, 300, 300) does not match the graph's static input (1, 3, 512, 512)`.
   Either the model is exported with dynamic spatial axes, or `tiles.tile_grid` must pad
   small rasters to the tile size and crop afterwards. Not a defect today; a WP4 decision.
3. **An SR Gate R must hash pixels, not file bytes.** Established in §2: identical pixel
   content produced 1,275,560,750-byte and 1,254,567,338-byte files under different
   `GDAL_CACHEMAX`. Since GDAL sizes its cache from installed RAM, a byte-identity gate would
   fail across machines for no real reason.
4. **The 8 px overlap threshold is a property of this scene and dtype, not a guarantee.**
   Measured on one 512×512 window of one uint8 scene. The default is 32 px, comfortably past
   it, but the threshold should not be quoted as a general result.
5. **Peak RSS is still ~2.66 GB for one scene at the default GDAL cache**, of which about
   1.64 GB is GDAL's block cache (2.639 GB default vs 1.003 GB at `GDAL_CACHEMAX=64`). It is
   fine on a 36 GB machine and will matter when WP4 adds an ONNX session alongside it;
   `--tile-px 256` roughly halves the accumulator's share.
6. **The 5 m output is unvalidatable against ground truth**, as WP0 recorded. Gate S asserts
   the grid, and tiling equivalence asserts the blend, but neither says the 5 m *values* are
   right, and nothing on this machine can. This must stay attached to any presentation of a
   5 m product.
7. **`--scale` accepts any power of two but only `s = 2` has been exercised end to end.**
   `s = 4` should run, and `require_integer_scale` refuses `s = 3` with a message explaining
   why, but neither has been measured.
8. **The registration's KF2 magnitude estimate was written for a different raster size than
   the case used** (§2). Harmless here, and left uncorrected because the registration is not
   edited after the fact — but a reminder that a predicted *number* needs its raster stated
   alongside it, not just a predicted verdict.
9. **A concurrent work package landed a reflectance corpus while this one was running, and
   the two have not been reconciled.** `tubitak/sr/docs/02a-reflectance-corpus.md` was
   written at 01:22:34 by a session other than this one — between this WP's registration
   (01:12) and its smoke run (01:33) — and reports 20 assets / 3,303,042,660 bytes acquired
   for the same five granules, with the four "not determined" product IDs of `00-recon.md`
   resolved (closing its risk R4). **Nothing in WP1 was read from or written to that work**,
   and its numbers are not independently checked here. Two consequences worth carrying:
   the WP1 fixed decision that the band domain stays an open question is now overtaken by
   events, and WP1's dtype/band-agnostic requirement stops being hypothetical — the relevant
   evidence is that `superresolve` was exercised on a synthetic **4-band uint16** source and
   produced a 4-band uint16 output over 9 tiles (§2 probe), so a reflectance input is a data
   change rather than a rewrite, as required. Whether the reflectance grid matches the TCI
   grid is that document's claim, not this one's, and WP3 should verify it against Gate S
   rather than inherit it.
